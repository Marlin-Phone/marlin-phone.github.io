source 'https://rubygems.org'
gem 'jekyll-paginate'

gem "jekyll", "~> 4.0"
gem "rake"

gem "webrick", "~> 1.7"

# Pin sass-embedded to a stable version to avoid build issues on CI
gem 'sass-embedded', '~> 1.93.2'
